# Tags, cover art, HTTPS
mutagen==1.48.1
pillow==12.3.0
certifi==2026.7.22

# iPod sync through iTunes (Windows only)
comtypes==1.4.16; sys_platform == "win32"

# AI vibe playlists: audio decoding (no ffmpeg needed) and the Claude API
soundfile==0.14.0
anthropic==1.8.0

# Audio tools: downloader, iTunes covers, spatial sound
requests==2.34.2
yt-dlp==2026.7.4
numpy==2.5.1
scipy==1.18.0
